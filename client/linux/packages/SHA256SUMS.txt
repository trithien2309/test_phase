# Linux PQC artifacts are intentionally not committed to git.
# Elastic Agent official artifact:
#   artifact = elastic-agent-9.4.2-linux-x86_64.tar.gz
#
# NCS custom artifact required for Linux standalone PQC:
#   artifact = filebeat-pqc-linux-amd64.zip
#
# Optional auditd bundle if you choose to package resources as one archive:
#   artifact = ncs-linux-auditd-v3.7.8-minimal.tar.gz
#
# Add hashes below only after the files are uploaded to the selected package storage.
